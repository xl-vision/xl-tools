sudo: required
language: node_js
node_js:
  - "10"
install: npm install
cache:
  directories: 
    - $HOME/.npm
jobs:
  include:
    # 代码检查
    - stage: lint
      script: npm run lint
stages:
  - lint